pipeline {
    agent any
    parameters { 
        choice(
            name: 'env', 
            choices: ['dev', 'stg', 'prd'], 
            description: '') 
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello stg'
            }
        }
    }
}
