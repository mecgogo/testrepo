properties([
    parameters([
        choice(
            name: 'env', 
            choices: 'stg\nprd', 
            description: '') ,
        choice(
            name: 'branch', 
            choices: 'main\nfeature', 
            description: '') 
    ])
])

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello '
            }
        }
    }
    post {
        always {
            script {
                manager.addShortText("${env}")
                manager.addShortText("${branch}")
            }
        }
    }
}
