pipeline {
    agent any
    parameters { 
        choice(
            name: 'env', 
            choices: ['stg', 'prd'], 
            description: '') 
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello prd'
            }
        }
    }
}
