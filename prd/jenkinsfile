properties([
    parameters([
        choice(
            name: 'ENV', 
            choices: 'stg\nprd', 
            description: ''),
        choice(
            name: 'branch', 
            choices: 'main\nfeature', 
            description: '') 
    ])
])

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo "Hello ${ENV}"
            }
        }
    }
    post {
        always {
            cleanWs()

            script {
                manager.addShortText("${ENV}", "black", "lightblue", "1px", "lightgray")
                manager.addShortText("${branch}", "black", "lightblue", "1px", "lightgray")
            }
        }
    }
}
