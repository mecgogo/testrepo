pipeline {
    agent any
    parameters { 
        choice(
            name: 'env', 
            choices: ['stg', 'prd'], 
            description: '') 
        choice(
            name: 'branch', 
            choices: ['main', 'feature'], 
            description: '') 
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello feature prd'
            }
        }
    }
}
