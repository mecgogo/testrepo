pipeline {
    agent any
    parameters { 
        choice(
            name: 'CHOICES', 
            choices: ['dev', 'stg', 'prd'], 
            description: '') 
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello prd'
            }
        }
    }
}
